/**
 * i18n adapter - provides translation stubs
 * Replace with FlowForge's i18n system
 */

/**
 * Translation function - returns the key as-is for now
 * Replace with actual i18n implementation
 */
export function t(key: string, _params?: Record<string, unknown>): string {
  return key
}
